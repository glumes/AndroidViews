<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.glumes.androidview">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".BezierFlowerActivity" />

        <activity android:name=".ui.InflaterActivity" />

        <activity android:name=".path.PathActivity" />

        <activity android:name=".imagebanner.ImageBannerActivity"></activity>

        <activity android:name=".customtoast.CustomToastActivity">

        </activity>
        <activity android:name=".customviews.ImageSliderActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>